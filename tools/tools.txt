tools folder will contain useful scripts and highly reused commands







using separate window in EC2 to keep scripts running even after connection closes:

tmux new -s mywindow

tmux a -t mywindow
